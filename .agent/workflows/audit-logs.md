---
description: 実行ログの監査とエラー原因の特定
---

1. **ログ集計を実行**:
   ```powershell
   npm run ops:audit
   ```
2. 出力された「エラー Top N」と「失敗件数」を要約して報告する。
3. 特に件数が多いエラーについて、`storage/app.db` の `task_runs` テーブルまたは `logs/` 配下の個別ログを調査し、具体的な原因を特定する。
4. 調査結果に基づき、再試行が必要か、コードやプリセットの修正が必要かをユーザーに提案する。
